<!--getting started-->
<div class="frame">
    <div class="box-1">
        <div class="paragraph">
            <h2 class="big-text">You are ONE step away from your first loan</h2>
            <p class="small-text">To have access to the best loan in the whole stae,<br> we first we need to get some information from You</p>
        </div>
        <div class="form">
            <form action="/register" method="post">
                <div class="name">
                    <label for="name">Enter your name</label>
                    <input type="text" placeholder="Enter your Name here" name="name"required>
                </div>
                <div class="password">
                    <label for="password">Enter your login password</label>
                    <input type="password" placeholder="Enter your password" name="password"required>
                </div>
                <div class="password">
                    <label for="name">Confirm password</label>
                    <input type="password" placeholder="Confirm password" name="password-1"required>
                </div>
                 <button type="submit">Create your account</button>
                    <a href="/login">I already have an account</a>
            </form>

                   
        </div>
    </div>
    <div class="box-2"></div>
</div>

